<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="ph.edu.ph.mobdeve.s18.baliog.miguel.countit">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.CountIt">
        <activity android:name=".ViewIntakeActivity"/>
        <activity android:name=".SetGoalsActivity" />
        <activity android:name=".LogActivityActivity" />
        <activity android:name=".RegisterActivity" />
        <activity android:name=".SplashScreenActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".LogMealActivity" />
        <activity android:name=".SetNewWeightActivity" />
        <activity android:name=".MainActivity" />
    </application>

</manifest>